version: '3.8'

services:
  resumock-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resumock-ai
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => {if (r.statusCode !== 200) process.exit(1);})"]
      interval: 30s
      timeout: 10s
      retries: 3
